bindAddresses = ::,0.0.0.0
port = 80
servePath = "/pac/"
dataDir = "/app/data"
saveDir = "/app/save"
serveCacheDir = "/app/servecache"
logDir = "/app/log"
accessLogToConsole = false
wwwDir = "/app/dist"

# enable HTTPS (docker image does not contain pacwebadmin.crt, pacwebadmin.key)
# certificateChainFile = "/etc/pacwebadmin/pacwebadmin.crt"
# privateKeyFile = "/etc/pacwebadmin/pacwebadmin.key"